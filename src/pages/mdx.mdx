---
description: This page was generated from an MDX file.
hero:
  caption:
    heading: Look at this page that was generated from an MDX file.
    subheading: MDX is an extension of markdown (.md) that lets you write JSX code and use React Components as shortcodes in markdown, just remember to update your file extension from .md to .mdx.
title: This is an MDX page
---

import { OutboundLink } from "gatsby-plugin-google-gtag";

<Pattern as="h2" mod="py-8 md:py-10 xl:py-12 w-full">
  This page is built with MDX
  <Pattern as="hr" is="divider" mod="mb-4 border-primary" />
  <Text pattern="meta" className="block">
    Dated 10/04/2020
  </Text>
</Pattern>

### This is an h3 in markdown syntax

**This is bold markdown text:**

You can still write your pages in plain markdown, mixed in with regular HTML with Taiwind CSS or custom CSS classes. It's up to you what you want to do. You can also imort other components at the top of the file if for some reason you don't want to add it to the components shortcode config insde the MDX component. Like I did with <OutboundLink className="underline font-medium" href="https://www.gatsbyjs.com/plugins/gatsby-plugin-mdx/" target="_blank">this outbound link you can click</OutboundLink> for more details.

But using imports means you'll have to style them yourself and you want to avoid that. So you should just use the <Link to="/">link component</Link> to create both inbound and outbound links. Using [Markdown Syntax](/jsx) to create links works just as well for both internal and [external links](https://www.gatsbyjs.com/) since it's already pre-configured to use the link component.

<Pattern as="hr" is="divider" mod="my-4 md:my-6 xl:my-8" />

<Text as="h5" pattern="title" mod="mb-6 mt-4">
  This is an h5 title text pattern with a margin modifier
</Text>

You can also write your own code in markdown, although that's not recommended you can technically do it. Go search for that though, I remember seeing an example a while back but I can't find it right now.

<Section is="content" isContained mod="py-8 md:py-10 xl:py-12">

#### This is a contained content section component with vertical padding

<Box className="gap-4 grid md:grid-cols-3 my-6">
  <Text as="p" className="md:mb-6">
    You can use component shortcodes
  </Text>
  <p className="md:mb-6">You can write regular HTML content.</p>
  <Element as="p" className="md:mb-6">
    Whatever components you pass to or define in MDX will be available as
    shortcodes.
  </Element>
</Box>

You can use regular markdown syntax, but not in a grid - have to wrap that in a div or something man. I mean give me something to work with here dude.

</Section>

###### This is a regular h6 headline

This is just a regular markdown paragraph. That's cool and all but shortcodes are even cooler, check some of them out:

<Text as="h4" pattern="title" mod="mt-8 mb-4 md:mt-10 md:mb-6 xl:mt-12 xl:mb-8">
  Look at all the different style buttons
</Text>

<Box flex="1" flow="row-wrap" className="items-center w-full">
  <Button mod="mb-4 mr-4">Default button</Button>
  <Pattern
    is="wrapper"
    mod="bg-background-secondary text-color-secondary border-secondary hover:bg-background-primary hover:text-color-primary hover:border-primary mb-4 mr-4"
  >
    <Button color="inherit">Inherit button</Button>
  </Pattern>
  <Button color="primary" mod="mb-4 mr-4">
    Primary button
  </Button>
  <Button color="secondary" mod="mb-4 mr-4">
    Secondary button
  </Button>
  <Pattern as="hr" is="divider" mod="mt-4 md:mt-6 xl:mt-8" />
  <Text as="h5" pattern="subheading" mod="mb-4 mt-8 w-full">
    Outline buttons
  </Text>
  <Button pattern="outline" mod="mb-4 mr-4">
    Default outline
  </Button>
  <Pattern
    is="wrapper"
    mod="bg-transparent text-background-primary border-background-primary hover:text-background-secondary hover:border-background-secondary mb-4 mr-4"
  >
    <Button color="inherit" pattern="outline">
      Inherit outline
    </Button>
  </Pattern>
  <Button color="primary" pattern="outline" mod="mb-4 mr-4">
    Primary outline
  </Button>
  <Button color="secondary" pattern="outline" mod="mb-4 mr-4">
    Secondary outline
  </Button>
  <Pattern as="hr" is="divider" mod="mt-4 md:mt-6 xl:mt-8" />
  <Text as="h5" pattern="subheading" mod="mb-4 mt-8 w-full">
    Text buttons
  </Text>
  <Button pattern="text" mod="mb-4 mr-4">
    Default text
  </Button>
  <Pattern is="wrapper" mod="text-color-hover hover:text-color mb-4 mr-4">
    <Button color="inherit" pattern="text">
      Inherit text
    </Button>
  </Pattern>
  <Button color="primary" pattern="text" mod="mb-4 mr-4">
    Primary text
  </Button>
  <Button color="secondary" pattern="text" mod="mb-4 mr-4">
    Secondary text
  </Button>
  <Pattern as="hr" is="divider" mod="mt-4 md:mt-6 xl:mt-8" />
  <Text as="h5" pattern="subheading" mod="mb-4 mt-8 w-full">
    Button sizes
  </Text>
  <Button size="xs" pattern="outline" mod="mb-4 mr-4">
    XS button
  </Button>
  <Button size="sm" pattern="outline" mod="mb-4 mr-4">
    SM button
  </Button>
  <Button size="md" pattern="outline" mod="mb-4 mr-4">
    MD button
  </Button>
  <Button size="lg" pattern="outline" mod="mb-4 mr-4">
    LG button
  </Button>
  <Button size="xl" pattern="outline" mod="mb-4 mr-4">
    XL button
  </Button>
  <Button size="2xl" pattern="outline" mod="mb-6 mr-6">
    2XL button
  </Button>
  <Button size="3xl" pattern="outline" mod="mb-8 mr-8">
    3XL button
  </Button>
  <Button size="4xl" pattern="outline" mod="mb-10 mr-10">
    4XL button
  </Button>
  <Button size="5xl" pattern="outline" mod="mb-12 mr-12">
    5XL button
  </Button>
</Box>

<Pattern as="hr" is="divider" mod="my-4 md:my-6 xl:my8" />

<Text as="h4" pattern="title" mod="mt-8 mb-4 md:mt-10 md:mb-6 xl:mt-12 xl:mb-8">
  Look at this CSS grid component
</Text>

<Grid cols="md:grid-cols-4" gap="gap-4 md:gap-5 xl:gap-6" mod="w-full">
  <Box className="p-2 md:p-3 xl:p-4">Box Column</Box>
  <Grid mod="p-2 md:p-3 xl:p-4">Grid Column</Grid>
  <Pattern is="grid" mod="md:col-span-2 p-2 md:p-3 xl:p-4" bgColor="secondary">
    Two Column Span Grid Pattern Column
  </Pattern>
  <Grid
    col="col-span-full"
    mod="p-2 md:p-3 xl:p-4"
    bgColor="paper"
    color="paper"
  >
    Full Span Grid Column
  </Grid>
</Grid>

<Hero
  as="section"
  of="fluid"
  bgColor="primary"
  color="primary"
  mod="mt-20"
  caption={{
    context: (
      <Text as="h4" pattern="body" mod="mt-2 md:mt-3 xl:mt-4">
        This is why this component is here, to save you from the pain of not
        having a hero when you need one.
      </Text>
    ),
    heading: "Sometimes you need a hero",
  }}
/>
